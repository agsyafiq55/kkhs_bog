// Add this to your existing tailwind.config.js
module.exports = {
  // ... other config
  theme: {
    extend: {
      // ... other extensions
      animation: {
        blob: "blob 15s infinite",
      },
    },
  },
  // ... other config
}